import 'package:flutter/material.dart';

class ReportDamageView extends StatelessWidget {
  const ReportDamageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
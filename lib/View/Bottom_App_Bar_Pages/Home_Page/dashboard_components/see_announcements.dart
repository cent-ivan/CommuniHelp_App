import 'package:flutter/material.dart';

class SeeAnnouncements {
  void announcemntDialog(BuildContext context) {
    showDialog(
      context: context, 
      builder: (context) {
        return SimpleDialog(

        );
      }
    );
  }
}